import { PickedStocksPipe } from './picked-stocks.pipe';

describe('PickedStocksPipe', () => {
  it('create an instance', () => {
    const pipe = new PickedStocksPipe();
    expect(pipe).toBeTruthy();
  });
});
